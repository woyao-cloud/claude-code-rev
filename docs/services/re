服务层文档 (docs/services/)

  1. services-layer-architecture.md - 服务层架构总览
  2. service-execution-flow.md - 服务执行流程详解
  3. service-modules-detail.md - 服务模块深度实现细节
覆盖的核心模块

  ┌─────────────────┬────────────────────────────────────┬───────────────────────────────────────────────────┐
  │      模块       │               源文件               │                       职责                        │
  ├─────────────────┼────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ API 客户端      │ src/services/api/client.ts         │ 多提供商客户端创建（直连/Bedrock/Vertex/Foundry） │
  ├─────────────────┼────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ 消息传输        │ src/services/api/claude.ts         │ 流式消息发送、工具调用、响应处理                  │
  ├─────────────────┼────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ 重试逻辑        │ src/services/api/withRetry.ts      │ 指数退避、529 处理、快速模式降级、持久重试        │
  ├─────────────────┼────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ Bootstrap API   │ src/services/api/bootstrap.ts      │ 启动数据获取、缓存管理                            │
  ├─────────────────┼────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ Files API       │ src/services/api/filesApi.ts       │ 文件上传/下载、分页列表、并行处理                 │
  ├─────────────────┼────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ Session Ingress │ src/services/api/sessionIngress.ts │ 会话日志持久化、乐观并发控制                      │
  └─────────────────┴────────────────────────────────────┴───────────────────────────────────────────────────┘

  流程图覆盖

  1. API 客户端创建流程 - 多提供商选择逻辑
  2. 消息发送时序图 - 流式请求/响应处理
  3. 重试逻辑决策树 - 错误分类和重试策略
  4. 快速模式降级流程 - 429/529 处理和缓存保护
  5. 文件上传/下载流程 - 并行处理和重试机制
  6. 会话日志持久化流程 - 乐观并发控制和 409 冲突解决
  7. 完整请求链路时序图 - 从 query 到 API 响应
  ---
  文档亮点

  包含的图表:
  - ASCII 流程图（启动流程、工具执行、命令执行、权限检查）
  - DOT 格式决策树和流程图
  - Mermaid 时序图
  - 完整调用时序图

  涵盖的核心模块:
  - 工具注册表、命令注册表、查询引擎
  - 工具编排、流式执行器
  - Analytics、API、MCP、LSP、Compact、Policy Limits、Remote Settings、Plugins、Memory

  详细分析的工具:
  - BashTool、AgentTool、FileEditTool、FileReadTool
  - SkillTool、Task 工具系列

  详细分析的服务:
  - Analytics 事件队列和 GrowthBook 集成
  - API 请求配置和流式处理
  - MCP 连接管理和 OAuth 处理
  - LSP 服务器管理
  - 自动压缩触发机制
  - 记忆提取和 Forked Agent 执行
