# Yao Code 项目结构总览文档

> 文档生成日期：2026-04-01  
> 项目版本：999.0.0-restored

---

## 项目概述

这是一个从 source map 逆向还原的 **Claude Code** 源码项目，主要用于研究 Yao Code 的内部架构。

**构建工具**: Bun 1.3.5+  
**语言**: TypeScript/JavaScript (ES Modules)  
**仓库类型**: 逆向还原研究项目

---

## 目录结构

```
claude-code-rev/
├── src/                          # 主要源代码目录
│   ├── bootstrap-entry.ts        # 启动入口点
│   ├── main.tsx                  # 主程序入口 (~790KB)
│   ├── commands.ts               # 命令注册表 (755 行)
│   ├── tools.ts                  # 工具注册表
│   ├── Tool.ts                   # 工具类型定义
│   ├── context.ts                # 上下文管理
│   ├── query.ts                  # 查询引擎
│   ├── Task.ts                   # 任务管理
│   ├── replLauncher.tsx          # REPL 启动器
│   ├── ink.ts                    # 终端 UI 渲染
│   │
│   ├── commands/                 # CLI 命令模块 (60+ 命令)
│   ├── tools/                    # 工具实现 (25+ 工具类)
│   ├── services/                 # 服务层
│   ├── utils/                    # 工具函数
│   ├── types/                    # TypeScript 类型定义
│   ├── constants/                # 常量定义
│   ├── state/                    # 状态管理
│   ├── bridge/                   # 远程桥接 (~40 文件)
│   ├── cli/                      # CLI 传输层
│   ├── assistant/                # 助手模式
│   ├── buddy/                    # 伙伴通知
│   └── bootstrap/                # 启动状态
│
├── shims/                        # 兼容层/降级实现 (7 个 NAPI 包)
├── docs/                         # 文档目录
├── package.json                  # 项目配置
├── README.md                     # 项目说明
├── AGENTS.md                     # 开发指南
└── CLAUDE.md                     # 结构说明
```

---

## 核心模块详解

### 1. 启动流程模块 (Bootstrap)

**核心文件**:
- `src/bootstrap-entry.ts` - 启动入口
- `src/bootstrapMacro.ts` - 启动宏
- `src/main.tsx` - 主程序

**启动流程**:

```
bootstrap-entry.ts
├── ensureBootstrapMacro()
└── import('./entrypoints/cli.tsx')
    └── main.tsx
        ├── 性能分析初始化 (profileCheckpoint)
        ├── MDM 设置预读 (startMdmRawRead)
        ├── Keychain 预取 (startKeychainPrefetch)
        ├── 特征标志加载 (feature())
        ├── 环境变量应用
        ├── 遥测初始化
        └── 策略限制加载
```

**快速路径优化** (在 main.tsx 中处理):

| 标志 | 用途 |
|------|------|
| `--version` | 零模块加载，直接输出版本 |
| `--dump-system-prompt` | 输出系统提示 |
| `--daemon` | 守护进程模式 |
| `--remote-control` | 远程控制模式 |
| `--background` | 后台模式 |
| `--templates` | 模板输出 |

---

### 2. 命令系统 (Commands)

**核心文件**: `src/commands.ts` (755 行)

**命令架构**:

```
命令注册表 (commands.ts)
├── 基础命令 (30+)
│   ├── /help - 帮助信息
│   ├── /clear - 清除历史
│   ├── /config - 配置管理
│   ├── /login - 登录
│   └── /logout - 登出
│
├── 功能命令 (20+)
│   ├── /skills - 技能管理
│   ├── /memory - 记忆管理
│   ├── /tasks - 任务管理
│   ├── /mcp - MCP 管理
│   └── /plugins - 插件管理
│
├── Git 命令
│   ├── /commit - Git 提交
│   ├── /review - 代码审查
│   ├── /branch - 分支管理
│   └── /commit-push-pr - 提交并创建 PR
│
└── 条件命令 (feature flag 控制)
    ├── /assistant - 助手模式 (KAIROS)
    ├── /bridge - 桥接模式 (BRIDGE_MODE)
    ├── /voice - 语音模式 (VOICE_MODE)
    └── /ultraplan - 超级计划 (ULTRAPLAN)
```

**命令类型定义**:

```typescript
type Command = 
  | { type: 'prompt' }      // 模型可调用，展开为文本
  | { type: 'local' }       // 本地执行，文本输出
  | { type: 'local-jsx' }   // 终端 UI 渲染 (Ink)
```

**动态加载机制**:

```typescript
// 条件性加载 (feature flag 控制)
const assistantCommand = feature('KAIROS')
  ? require('./commands/assistant/index.js').default
  : null

// 技能动态加载
const { skillDirCommands, pluginSkills, bundledSkills } = await getSkills(cwd)
```

**命令可用性检查**:

```typescript
// 基于认证状态过滤
export function meetsAvailabilityRequirement(cmd: Command): boolean {
  if (!cmd.availability) return true
  for (const a of cmd.availability) {
    switch (a) {
      case 'claude-ai':
        if (isClaudeAISubscriber()) return true
        break
      case 'console':
        if (!isClaudeAISubscriber() && !isUsing3PServices()) return true
        break
    }
  }
  return false
}
```

---

### 3. 工具系统 (Tools)

**核心文件**: 
- `src/tools.ts` - 工具注册表
- `src/Tool.ts` - 工具类型定义

**工具架构**:

```
工具系统
├── 基础工具 (9 个)
│   ├── BashTool - 执行 shell 命令
│   ├── FileReadTool - 读取文件
│   ├── FileWriteTool - 写入文件
│   ├── FileEditTool - 编辑文件
│   ├── GlobTool - 文件搜索
│   ├── GrepTool - 内容搜索
│   ├── NotebookEditTool - Jupyter 编辑
│   ├── WebFetchTool - 网页抓取
│   └── WebSearchTool - 网络搜索
│
├── 高级工具 (16+ 个)
│   ├── AgentTool - 启动子代理
│   ├── SkillTool - 调用技能
│   ├── TaskCreateTool - 创建任务
│   ├── TaskUpdateTool - 更新任务
│   ├── TodoWriteTool - TODO 管理
│   ├── LSPTool - 语言服务器
│   ├── EnterPlanModeTool - 计划模式
│   ├── EnterWorktreeTool - 工作树模式
│   ├── ExitWorktreeTool - 退出工作树
│   ├── AskUserQuestionTool - 用户提问
│   └── ConfigTool - 配置工具
│
├── MCP 工具
│   ├── ListMcpResourcesTool - 列出 MCP 资源
│   └── ReadMcpResourceTool - 读取 MCP 资源
│
└── 条件工具 (feature flag 控制)
    ├── REPLTool - REPL 模式 (USER_TYPE=ant)
    ├── SleepTool - 休眠工具 (PROACTIVE/KAIROS)
    ├── Cron 工具集 - 定时任务 (AGENT_TRIGGERS)
    └── TerminalCaptureTool - 终端捕获 (TERMINAL_PANEL)
```

**工具接口定义**:

```typescript
// src/Tool.ts
export interface Tool {
  name: string
  description: string
  inputSchema: ToolInputJSONSchema
  execute: (args: any, context: ToolContext) => Promise<ToolResult>
  isEnabled: () => boolean
}

export interface ToolContext {
  cwd: string
  session: SessionState
  permissions: PermissionContext
  // ...
}
```

**工具预设**:

```typescript
export const TOOL_PRESETS = ['default']

export function getToolsForDefaultPreset(): string[] {
  const tools = getAllBaseTools()
  const isEnabled = tools.map(tool => tool.isEnabled())
  return tools.filter((_, i) => isEnabled[i]).map(tool => tool.name)
}
```

---

### 4. 服务层 (Services)

| 服务目录 | 核心功能 | 关键文件 |
|----------|----------|----------|
| `analytics/` | 分析埋点 | growthbook.ts, statsig.ts |
| `api/` | API 客户端 | bootstrap.ts, filesApi.ts |
| `mcp/` | MCP 协议 | client.ts, config.ts |
| `plugins/` | 插件系统 | pluginLoader.ts |
| `skills/` | 技能系统 | loadSkillsDir.ts |
| `lsp/` | 语言服务器 | manager.ts |
| `policyLimits/` | 策略限制 | index.ts |
| `remoteManagedSettings/` | 远程设置 | index.ts |

---

### 5. 状态管理 (State)

**核心文件**: 
- `src/bootstrap/state.ts` - 全局状态
- `src/state/store.ts` - 状态存储
- `src/state/AppStateStore.ts` - 应用状态

**状态架构**:

```typescript
interface AppState {
  session: {
    id: string
    model: string
    messages: Message[]
  }
  tools: {
    available: Tool[]
    permissions: PermissionState
  }
  settings: {
    theme: string
    keybindings: object
  }
}
```

**状态变更处理**:

```typescript
// src/state/onChangeAppState.ts
export function onChangeAppState<T>(
  selector: (state: AppState) => T,
  onChange: (newValue: T) => void
): void {
  // 订阅状态变更
}
```

---

### 6. 工具函数 (Utils)

**主要模块**:

| 模块 | 功能 |
|------|------|
| `auth.js` | OAuth 认证，令牌管理 |
| `config.js` | 全局配置，本地配置 |
| `model/model.js` | 模型选择，提供商路由 |
| `permissions/` | 权限系统，权限模式 |
| `settings/` | 设置系统，MDM 集成 |
| `plugins/` | 插件加载，版本管理 |
| `git.js` | Git 操作，分支管理 |
| `worktree.js` | Git 工作树 |
| `sessionStorage.js` | 会话存储 |
| `telemetry/` | 遥测埋点 |

---

### 7. Bridge 模块 (远程桥接)

**目录**: `src/bridge/` (~40 文件)

Bridge 模块实现远程控制协议，允许从移动设备/Web 端控制本地 Yao Code 会话。

**核心文件**:

| 文件 | 功能 |
|------|------|
| `bridgeApi.ts` | Bridge API 定义 |
| `bridgeConfig.ts` | Bridge 配置 |
| `bridgeMain.ts` | Bridge 主入口 |
| `bridgeMessaging.ts` | 消息传递 |
| `replBridge.ts` | REPL 桥接 |
| `replBridgeTransport.ts` | 传输层 |
| `jwtUtils.ts` | JWT 工具 |
| `webhookSanitizer.ts` | Webhook 清理 |

**远程安全命令**:

```typescript
// commands.ts - 远程模式安全命令
export const REMOTE_SAFE_COMMANDS: Set<Command> = new Set([
  session,    // 显示远程会话 QR
  exit,       // 退出 TUI
  clear,      // 清除屏幕
  help,       // 帮助
  theme,      // 主题
  cost,       // 会话成本
  plan,       // 计划模式
  // ... 共 17 个命令
])
```

---

### 8. CLI 传输层

**目录**: `src/cli/`

| 文件 | 功能 |
|------|------|
| `transports/Transport.ts` | 传输接口 |
| `transports/SSETransport.ts` | SSE 传输 |
| `transports/WebSocketTransport.ts` | WebSocket |
| `transports/HybridTransport.ts` | 混合传输 |
| `transports/SerialBatchEventUploader.ts` | 批量上传 |
| `remoteIO.ts` | 远程 I/O |
| `structuredIO.ts` | 结构化 I/O |

---

### 9. 架构模式

#### 特征标志系统 (Feature Flags)

```typescript
import { feature } from 'bun:bundle'

// 条件性功能
if (feature('DAEMON')) {
  // 守护进程功能
}

if (feature('KAIROS')) {
  // 助手模式功能
}

if (feature('WORKFLOW_SCRIPTS')) {
  // 工作流脚本
}
```

#### 懒加载模式

```typescript
// 延迟导入重型模块
const real = (await import('./commands/insights.js')).default

// 条件性 require
const assistantModule = feature('KAIROS') 
  ? require('./assistant/index.js') 
  : null
```

#### 记忆化优化

```typescript
import memoize from 'lodash-es/memoize.js'

// 命令列表记忆化
const COMMANDS = memoize((): Command[] => [...])

// 技能加载记忆化
const getSkills = memoize(async (cwd: string) => {...})
```

---

## 依赖关系

### 核心依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| `@anthropic-ai/sdk` | * | Anthropic API 客户端 |
| `@modelcontextprotocol/sdk` | * | MCP 协议 |
| `ink` | * | React 终端 UI |
| `@commander-js/extra-typings` | * | CLI 命令解析 |
| `chalk` | * | 终端颜色 |
| `lodash-es` | * | 工具函数 |
| `zod` | * | 类型验证 |
| `react` | * | UI 框架 |
| `ws` | * | WebSocket |
| `yaml` | * | YAML 解析 |

### 本地 Shim 包

| 包 | 路径 | 用途 |
|----|------|------|
| `@ant/claude-for-chrome-mcp` | `shims/ant-claude-for-chrome-mcp` | Chrome MCP |
| `@ant/computer-use-mcp` | `shims/ant-computer-use-mcp` | Computer Use MCP |
| `@ant/computer-use-swift` | `shims/ant-computer-use-swift` | Computer Use Swift |
| `color-diff-napi` | `shims/color-diff-napi` | 颜色差异 (NAPI) |
| `modifiers-napi` | `shims/modifiers-napi` | 修饰键 (NAPI) |
| `url-handler-napi` | `shims/url-handler-napi` | URL 处理 (NAPI) |

---

## 运行方式

### 环境要求

- Bun 1.3.5 或更高版本
- Node.js 24 或更高版本

### 基本命令

```bash
# 安装依赖
bun install

# 运行 CLI
bun run dev

# 查看版本
bun run version

# 查看帮助
bun run dev --help
```

### 开发模式

```bash
# 使用恢复检查入口
bun run dev:restore-check
```

---

## 项目状态

### 已恢复功能

- ✅ `bun install` 成功
- ✅ `bun run version` 成功
- ✅ `bun run dev` 通过真实 CLI bootstrap 启动
- ✅ 默认 Bun 脚本走真实 CLI 路径
- ✅ bundled skill 内容恢复为可用文档
- ✅ Chrome MCP 和 Computer Use MCP 兼容层恢复

### 已知限制

- ⚠️ 部分模块仍包含恢复时的 fallback
- ⚠️ 类型文件可能缺失
- ⚠️ 构建生成文件可能不完整
- ⚠️ 原生模块使用降级实现

---

## 扩展开发

### 添加新命令

1. 在 `src/commands/` 创建新目录
2. 实现命令逻辑并导出 `default`
3. 在 `src/commands.ts` 中注册

### 添加新工具

1. 在 `src/tools/` 创建新目录
2. 实现工具类，继承基础工具接口
3. 在 `src/tools.ts` 中注册

### 添加新服务

1. 在 `src/services/` 创建新目录
2. 实现服务接口
3. 在 `src/entrypoints/init.ts` 中初始化

---

## 相关文件

- `README.md` - 项目说明和运行方式
- `package.json` - 项目配置和依赖
- `src/bootstrap-entry.ts` - 启动入口
- `src/commands.ts` - 命令注册表
- `src/tools.ts` - 工具注册表
- `src/Tool.ts` - 工具类型定义
